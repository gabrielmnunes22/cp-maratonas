#include <bits/stdc++.h>
#include <numbers>
using namespace std;
int main(){
    int n;
    if (!(cin >> n)) return 0;
    double pis = numbers::pi;
    cout << fixed << setprecision(n) << "pi." << pis << '\n';
}
